mcpServers:
  wikidata_stdio:
    type: stdio
    command: uv
    args: [ "run", "mcp-wikidata" ]
    cwd: "${MCP_WIKIDATA_DIR}"

  wikipedia_stdio:
    type: stdio
    command: uv
    args: [ "run", "wikipedia-mcp" ]
    cwd: "${MCP_WIKIPEDIA_DIR}"
